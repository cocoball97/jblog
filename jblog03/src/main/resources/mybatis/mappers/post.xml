<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="post">

	<select id="findPost" parameterType="string" resultType="postvo">
		<![CDATA[
		  select b.contents, b.title, b.reg_date, b.category_id, b.id
		    from category a 
	    	join post b on a.id = b.category_id
	       where a.blog_id = #{blog_id}
        order by b.reg_date DESC
	       limit 5
		]]>	
	</select>
	
	<select id="findLatestPost" parameterType="string" resultType="postvo">
    <![CDATA[
		  select b.contents, b.title, b.reg_date, b.category_id, b.id
		    from category a
		    join post b on a.id = b.category_id
		   where a.blog_id = #{blog_id}
		   order by b.reg_date DESC
		   limit 1
    ]]>
</select>
	
</mapper>